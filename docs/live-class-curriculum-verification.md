# Verification

Run `npm test`, `npm run lint`, and `npm run build` before merging. After deployment, open an active class dashboard and confirm that its sessions have topic and curriculum ID fields in both classSessions and attendance session records.
